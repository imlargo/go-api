package content

// Shotstack text rendering configuration constants
const (
	// BaseFontSize is the base font size for text overlays in Shotstack
	BaseFontSize = 68

	// BaseLineHeight is the base line height multiplier for text overlays
	BaseLineHeight = 0.8

	// MaxTextWidth is the maximum width for text overlays in pixels
	MaxTextWidth = 900

	// ApproxCharWidth is the approximate character width for Montserrat ExtraBold font
	ApproxCharWidth = 25

	// FontReductionFactor is the factor by which font size is reduced per character over the limit
	FontReductionFactor = 0.046

	// LineReductionFactor is the factor by which line height is reduced per character over the limit
	LineReductionFactor = 0.025
)
